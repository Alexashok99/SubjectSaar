
https://youtu.be/V7WpadAi3RI?si=x0dro1ae7uicFpBC
ctrl+shift+p :- search command (type- git clone)

git --version

set username and email

upload full project on gitHub
-----------START-------------------------------------
step 1
git config --global user.name "Alexashok99"
git config --global user.email "alexashok999@gmail.com"
step 2
touch .gitignore
step 3
git init
step 4
git add .
step 5
git commit -m "Upload Project"
step 6
git status
step 7
git remote add origin https://github.com/Alexashok99/FileManager.git
git remote add origin https://github.com/Alexashok99/SubjectSaar.git
step 8
git push origin master

--------------END--------------------------------------
git config --list


git clone "https//abc"

git status

1. git add <file name>

 git add .                               (all add)

2. git cimmit -m "First Commit"

3. git push origin main          (pushing)

case 1: staged changes
git reset <file name>
git reset                                 all

case 2: commited changes (for one commit)
git reset HEAD~1

case 3: commited changes ( for many commits)
git reset <commit hash>
git reset --hard <commit hash>


fork - clone other repo to your account



Here’s a table of main **Git commands** with short descriptions:

| Command                   | Description                                                   |
| ------------------------- | ------------------------------------------------------------- |
| `git init`                | Initialize a new Git repository in the current folder.        |
| `git clone <url>`         | Copy (clone) an existing remote repository to your system.    |
| `git status`              | Show the current status of changes in the repository.         |
| `git add <file>`          | Stage a specific file for commit.                             |
| `git add .`               | Stage all changes in the current directory.                   |
| `git commit -m "message"` | Save staged changes with a commit message.                    |
| `git log`                 | View commit history.                                          |
| `git branch`              | List branches in the repository.                              |
| `git branch <name>`       | Create a new branch.                                          |
| `git checkout <branch>`   | Switch to another branch.                                     |
| `git merge <branch>`      | Merge a branch into the current branch.                       |
| `git pull`                | Fetch and merge changes from the remote repository.           |
| `git push`                | Upload local commits to the remote repository.                |
| `git remote -v`           | Show remote repository URLs.                                  |
| `git diff`                | Show changes between commits, branches, or working directory. |

If you want, I can also make a **Git command flow chart** so it’s easier to remember.



Start Project
   ├── git init                # Create a new local repo
   └── git clone <url>         # Copy remote repo

Make Changes
   └── Edit files

Stage Changes
   └── git add <file> / git add .

Commit Changes
   └── git commit -m "message"

Work with Branches
   ├── git branch <name>
   └── git checkout <branch>

Merge Branch
   └── git merge <branch>

Update from Remote
   └── git pull

Push to Remote
   └── git push

